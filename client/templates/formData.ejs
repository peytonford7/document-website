<table class="table table-sm table-striped">
	<thead>
		<tr>
			<th>Key</th>
			<th>Value</th>
		</tr>
	<thead>
	<tbody>
		<% for (const [key, value] of Object.entries(formData)) { %>
			<tr>
				<td><%= key %></td>
				<td><%= value %></td>
			</tr>
		 <% } %>	
	</tbody>
</table>
